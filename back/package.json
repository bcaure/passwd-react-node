{
  "name": "passwd-back-nodejs",
  "version": "1.0.0",
  "description": "Passwd backend with NodeJS",
  "main": "server.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "debug": "nodemon --inspect server.js",
    "start": "node --env-file=.env server.js",
    "migrate:passwords": "node --env-file=.env migrate-encrypt-passwords.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Benjamin CAURE",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "body-parser": "^2.2.2",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.1",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.22.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
