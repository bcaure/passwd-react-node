# Fake values for local/test runs only — never use in production
DATASOURCE_HOST=127.0.0.1
DATASOURCE_USER=passwd
DATASOURCE_PASSWORD=passwd
DATASOURCE_DATABASE=passwd
SECRET=dev-only-fake-jwt-secret-not-for-production
PORT=3001
